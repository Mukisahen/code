import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.farmbhade.app',
  appName: 'Farm Bhade',
  webDir: 'dist',
  backgroundColor: '#FFFFFF',
  android: {
    backgroundColor: '#FFFFFF',
  },
}

export default config
